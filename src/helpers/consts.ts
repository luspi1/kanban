export enum AppRoute {
	Home = '/',
}

export enum TaskStatusMap {
	high = 'высший',
	medium = 'средний',
	common = 'обычный',
	lower = 'на потом',
}

export enum ItemType {
	TASK = 'Task',
}

export enum NameSpace {
	Tasks = 'TASKS',
}
